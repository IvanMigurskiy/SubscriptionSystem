version: "3.9"

services:
  subscr_system:
    image: subscr/subscr_system_image:latest
    container_name: subscr_system
    restart: unless-stopped
    networks:
      - subscr-net
    depends_on:
      - postgres
    env_file:
      - ${PWD}/.env

  postgres:
    image: postgres:17.2
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB_NAME}
      PGDATA: /var/lib/postgresql/data/pgdata
    networks:
      - subscr-net
    volumes:
      - posgtres_data:/var/lib/postgresql/data/pgdata

volumes:
  posgtres_data:
    driver: local

networks:
  subscr-net:
    driver: bridge